const colors = require('colors');

const stringOne = 'This is a plain string.';

console.log(colors.stylize(stringOne, 'blue'));
console.log(colors.stylize(stringOne, 'green'));
console.log(colors.stylize(stringOne, 'yellow'));
